{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/marketing && NODE_ENV=production pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
