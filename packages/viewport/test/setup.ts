// Test setup for viewport

import { beforeAll, afterAll } from 'vitest';

// Mock WebGL context if needed
beforeAll(() => {
  // Setup test environment
  console.log('Setting up viewport tests...');
});

afterAll(() => {
  // Clean up
  console.log('Cleaning up viewport tests...');
});
