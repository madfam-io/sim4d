{
  "name": "@brepflow/schemas",
  "version": "0.1.0",
  "description": "BrepFlow JSON schemas",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "validate": "ajv compile -s schemas/*.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@brepflow/types": "workspace:*",
    "@types/three": "^0.180.0",
    "ajv": "^8.12.0",
    "three": "^0.181.2",
    "three-stdlib": "^2.36.1"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
